;; aiueos system graph — the set of components that make up this running system.
;; `aiueos inspect examples/system.aiueos.edn` to see the capability graph;
;; `aiueos verify examples/system.aiueos.edn --policy examples/policy/default.edn`.
{:aiueos/system :demo
 :aiueos/components ["components/log-service.edn"
                   "components/fs-service.edn"
                   "drivers/virtio-blk.edn"
                   "apps/notes.edn"]}
